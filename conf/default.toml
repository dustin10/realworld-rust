signing_key = "default-signing-key"

[http]
port = "7100"

[database]
user = "postgres"
password = ""
url = "localhost:5432"
name = "postgres"
max_connections = "50"
connection_timeout = "60"

[kafka]
servers = "localhost:29092"

[outbox]
interval = 1000
batch_size = 10
